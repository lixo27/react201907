import React from 'react';

const JournalListGroup = ( { children } ) => {
    return (
        <div className="list-group">
            { children }
        </div>
    );
};

export default JournalListGroup;
